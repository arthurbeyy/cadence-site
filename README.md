# CADENCE, site vitrine

Site en ligne sur https://cadence-reporting.fr, publié automatiquement par GitHub Pages à chaque modification de la branche `main`.

Site statique, sans outil de build : `index.html`, `styles.css`, `app.js`.
